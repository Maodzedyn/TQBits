@model List<TQBits.Context.Candidate>
@{
    ViewBag.Title = "CandidateList";
}

<h2>CandidateList</h2>

@*<table class="table table-hover">
        <tr>
            <th>Name</th>
            <th>Phone</th>
            <th>Date of birth</th>
            <th>Married</th>
            <th>Salary</th>
        </tr>
        @foreach (var item in Model)
        {

            <tr>
                <td>@item.Name</td>
                <td>@item.Phone</td>
                <td>@item.DateOfBirth</td>
                <td>@item.Married</td>
                <td>@item.Salary</td>
                <td>
                    <a href="@Url.Action("Candidate", new {item.Id, item.Name, item.Phone, item.DateOfBirth, item.Married, item.Salary })" class="btn btn-success">Edit</a>
                    <a href="@Url.Action("Delete", new { item.Id })" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </table>*@

<table id="dtBasicExample" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th class="th-sm">
                Name
            </th>
            <th class="th-sm">
                Phone
            </th>
            <th class="th-sm">
                Date of birth
            </th>
            <th class="th-sm">
                Married
            </th>
            <th class="th-sm">
                Salary
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Phone</td>
                <td>@item.DateOfBirth</td>
                <td>@item.Married</td>
                <td>@item.Salary</td>
                <td>
                    <a href="@Url.Action("Candidate", new {item.Id, item.Name, item.Phone, item.DateOfBirth, item.Married, item.Salary })" class="btn btn-success">Edit</a>
                    <a href="@Url.Action("Delete", new { item.Id })" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<script>
    $(document).ready(function () {
        $('#example').DataTable();
    });
</script>














